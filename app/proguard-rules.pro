# Add project specific ProGuard rules here.
# We will fill this in properly when we enable release minification in a later step
# (important once the video engine / AI SDKs are added, since native + reflection-based
# libraries often need explicit keep rules).
