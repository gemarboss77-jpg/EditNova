<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        No runtime permissions are requested. Media selection (Step 3) uses the
        Android Photo Picker (ActivityResultContracts.PickVisualMedia) instead of
        READ_MEDIA_IMAGES/READ_MEDIA_VIDEO — the system handles the media browsing UI
        and only grants the app access to the single item the user actually picks, so
        no storage permission is needed for that flow.
        Future steps may still add permissions like INTERNET (Firebase, subscriptions)
        when the feature that needs them is actually built.
    -->

    <application
        android:name=".EditNovaApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.EditNova"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.EditNova"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
